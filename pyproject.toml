[project]
name = "proton-local-toolkit"
version = "0.1.0"
description = "Local-first toolkit for batch media, text, and file processing."
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pillow>=10.0",
]

[tool.uv]
package = false
